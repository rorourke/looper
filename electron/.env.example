# HTTPS origin used for the narrowly scoped live market-price endpoint.
# Do not include `/api/v1`.
MAIN_VITE_LOOPER_API_URL=https://looper.app

# Release builds set this to `stable`. Keep it disabled for local packages.
MAIN_VITE_UPDATE_CHANNEL=disabled
